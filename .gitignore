# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor
.vscode/
.idea/

# Backups (kept locally, not pushed)
*.bak
*.bak.*

# Raw scraped corpus — re-derivable via scripts/fetch_*.py
# (Excluded to keep repo lean; full PDFs + HTML are 110+ MB)
data/raw/

# Local Catala/opam toolchain artefacts (if ever installed)
_build/
_targets/
*.cmo
*.cmi
*.cmx
*.o
*.cma
*.cmxa
*.a

# Dashboard server lockfiles
dashboard/.server.pid

# Client build artefacts
clients/python/build/
clients/python/dist/
clients/python/*.egg-info/
clients/typescript/node_modules/
clients/typescript/dist/

# Perturbation script outputs — only commit when populated by a real
# (non-dry-run) API run. The scripts also emit these on --dry-run with
# status="would_grade"; do not ship dry-run state to the public repo.
data/sicc_pr4_recoded.json
data/robustness/test_retest.json
data/robustness/test_retest_summary.json
data/robustness/tribunal_blind.json
data/robustness/tribunal_blind_summary.json
data/robustness/model_size.json
data/robustness/model_size_summary.json
data/robustness/prompt_rephrase.json
data/robustness/prompt_rephrase_summary.json
data/robustness/sub_rubric_proposed.json
data/robustness/sub_rubric_scores.json
data/robustness/sub_rubric_summary.json
