# SICC stratified-expansion plan (n=13 → n≥75)

> Generated by `scripts/plan_sicc_expansion.py`. **STATUS: SUPERSEDED — direct elitigation.sg pull executed 2026-05 via `scripts/fetch_sicc_direct.py`; SICC corpus expanded from n=13 to n=80.** Plan retained as historical artefact. See `data/tos_audit.md` §3 for the ToS posture under which the direct pull operates.

## Current distribution (n=13)

| claim_type                | n |
|---------------------------|--:|
| arbitration_recognition   | 7 |
| costs_assessment          | 2 |
| substantive_breach        | 2 |
| arbitration_enforcement   | 1 |
| interim_relief            | 1 |

## Mapped to target strata

| Stratum                                    | Have | Need | Gap |
|--------------------------------------------|-----:|-----:|----:|
| Substantive commercial                     |    2 |   25 |  23 |
| Jurisdiction / recognition / enforcement   |    8 |   25 |  17 |
| Costs / case management                    |    3 |   25 |  22 |
| **Total gap**                              |      |      | **62** |

## Source listing URLs (for permitted pull)

The eLitigation listing for SICC is paginated; the existing `scripts/fetch_sicc_more.py` already targets page 2. To reach n≥75, expand the page range to 1–N (with N depending on output volume). Listing pattern:

```
https://www.elitigation.sg/gd/Home/Index?Filter=SICC&YearOfDecision=All&SortBy=DateOfDecision&CurrentPage={N}&SortAscending=False&PageSize=0&Verbose=False&SearchQueryTime=0&SearchTotalHits=0&SearchMode=True&SpanMultiplePages=False
```

## Pre-conditions before scrape

1. Written permission from the Singapore Courts registrar covering academic / research redistribution, OR a confirmed scope of fair-dealing-only redistribution in `LICENSE` (see `data/tos_audit.md` §3).
2. `FIRECRAWL_KEY` env var set, or replace `fetch_sicc_more.py` with a direct-fetch implementation (eLitigation does not require an API key for individual judgment pages).
3. Stratification target list written to `data/sicc_pull_targets.json` so the fetcher hits judgments evenly across the three strata.

## Re-scoring step

After fetch, run:

```bash
python3 scripts/triage_sicc.py
python3 scripts/grade_borderline.py     # SICC subset
python3 scripts/merge_sicc.py
python3 scripts/_verify_sicc_merge.py
```

Then update §4 of `paper.md` with revised SICC means and re-run `python3 scripts/analyse_falsification.py` and `python3 scripts/analyse_comparison.py` so the courts row reflects the new SICC n.
